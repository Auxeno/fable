[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fable"
version = "0.1.0"
description = "Educational, from-scratch JAX GPT stack trained on the TinyStories dataset."
readme = "readme.md"
requires-python = ">=3.10"
authors = [{ name = "Alex Goddard" }]
dependencies = [
    "jax",
    "flax",
    "optax",
    "orbax-checkpoint",
    "tqdm",
]

[project.optional-dependencies]
dev = [
    "black",
    "ipykernel",
    "pytest",
]

[project.scripts]
fable-generate = "fable.generate:main"
fable-train = "fable.train:main"
fable-prepare-data = "fable.data.pipeline:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["fable*"]
exclude = ["notebooks", "scripts", "checkpoints", "data", "experiments.ipynb"]

[tool.pytest.ini_options]
pythonpath = ["."]
